import {Paper, Row, Typography} from "../../shared";

export const User = () => {
  return (
    <Row className="justify-center">
      <Paper>
          <Typography
            level={1}
            center
            >
            User
            </Typography>
      </Paper>
    </Row>
  );
};
